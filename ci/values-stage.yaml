replicaCount: 1
route:
  host: app-stage.15.224.195.86.sslip.io
env:
  APP_URL: https://app-stage.15.224.195.86.sslip.io
efs:
  enabled: false
mysql:
  primary:
    persistence:
      storageClass: local-path
      selector: null
backup:
  enabled: false
sealedSecrets:
  mysqlCredentials:
    encryptedData:
      app-key: stage-placeholder
      mysql-password: stage-placeholder
      mysql-root-password: stage-placeholder
  ghcrPullSecret:
    encryptedData:
      .dockerconfigjson: stage-placeholder
